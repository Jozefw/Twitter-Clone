<h1>Tweets#index</h1>
<p>Find me in app/views/tweets/index.html.erb</p>

<% @tweets.each do |tweet| %>
  <%= tweet.post %> :
  <%= tweet.comment %> <%= link_to "Edit", edit_tweet_path(tweet.id) %> <%= link_to"Delete", tweet_path(tweet.id), method: :delete %> <br><br>
<% end %>

<%= link_to'Create a Tweet', new_tweet_path %>
